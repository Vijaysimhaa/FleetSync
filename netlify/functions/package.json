{
  "name": "netlify-functions",
  "version": "1.0.0",
  "description": "Netlify Functions for FleetSync backend",
  "main": "getDashboardData.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^12.0.0"
    // Make sure the version here is compatible. You can check
    // the latest compatible version on npmjs.com/package/firebase-admin
  }
}
